// import styled from "styled-components"; // You need this as well
// import { generateMedia } from "styled-media-query";
import createBreakpoints, { BreakpointValues } from "@material-ui/core/styles/createBreakpoints";

export const breakpointValue: BreakpointValues = {
  xs: 0,
  sm: 600,
  md: 960,
  lg: 1280,
  xl: 1920,
};
